# Ignore node_modules folder
node_modules/

# Ignore build output folder
dist/

# Ignore logs
logs/
*.log

# Ignore .env files which might contain sensitive information
.env

# Ignore OS generated files
.DS_Store
Thumbs.db
# Ignore graphify tool output
graphify-out/

coverage/
*.tsbuildinfo

# Packed tarballs
*.tgz

# Playwright MCP scratch output
.playwright-mcp/

# SDD workspace: ledger, review packages, agent reports — scratch, never published
.superpowers/

# Internal planning artefacts — not part of the published project

# Remember plugin session scratch — never publish
.remember/

# Local agent/tool state — never publish
.claude/
.codegraph/

# Raw event capture: intermediate artefact for building test/fixtures/events/*,
# never redacted, may contain real device ids and stream tokens — never commit
test/fixtures/events/raw-capture.json
